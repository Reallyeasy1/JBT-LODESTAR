# /review-pr

Thin wrapper for backward compatibility. Delegates entirely to the `pr-review` skill.

Arguments: `$ARGUMENTS`

Invoke the `pr-review` skill with the arguments passed: `$ARGUMENTS`

See `.claude/skills/pr-review/SKILL.md` for the full implementation.
