# /pr-review — PR Code Reviewer

Performs a structured code review of the current branch against a base branch. Read-only by default. Never edits source files or posts comments unless `--post` is passed and confirmed.

Arguments: `$ARGUMENTS`

Invoke the `pr-review` skill with arguments: `$ARGUMENTS`

See `.claude/skills/pr-review/SKILL.md` for the full implementation.
