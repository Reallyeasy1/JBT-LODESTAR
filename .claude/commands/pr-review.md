# /pr-review — PR Code Reviewer

Performs a structured code review of the current branch against a base branch. After producing the review, offers to post findings directly to the GitHub PR. Pass `--post` to skip the confirmation prompt.

Arguments: `$ARGUMENTS`

Invoke the `pr-review` skill with arguments: `$ARGUMENTS`

See `.claude/skills/pr-review/SKILL.md` for the full implementation.
